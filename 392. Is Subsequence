class Solution:
    def isSubsequence(self, s: str, t: str) -> bool:
        s = list(s)
        t = list(t)
        count=0
        for i in s:
            if count >= 1:
                t = t[x+1:]
            for x in range(0, len(t)):
                if i == t[x]:
                    count +=1
                    break
            
        if count == len(s):
            return True
        else:
            return False


        
